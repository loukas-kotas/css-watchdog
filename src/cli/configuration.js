


const facade = require('../logic/facade');
const errorHandler = require('../logic/error-handler.controller');
const fs = require('fs');

// var contents = fs.readFileSync("./cli-config.json");
// var conf = JSON.parse(contents);


const run_through_configuration = async () => {
    // handleCases(conf);
}

module.exports = {
    run_through_configuration: run_through_configuration
}